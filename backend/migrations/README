Generic single-database configuration with async support for SWESphere.

To create a new migration:
    alembic revision --autogenerate -m "description"

To apply migrations:
    alembic upgrade head

To rollback:
    alembic downgrade -1

